//please after pulling , you could find issues with UI depencies :
2️⃣ Install project dependencies :

   npm install
   
3️⃣ Install required UI libraries (compatible with React 19)
//To ensure smooth functionality and avoid peer dependency conflicts, run:

npm install framer-motion@12.23.24 --save
npm install @tabler/icons-react@latest --force
npm install @material-tailwind/react@2.1.10 --save
npm install gsap --save
npm install scrollreveal --save